pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh """echo 'Hello World!!!'
                    echo 'Multiline shell steps works too'"""
            }
        }
    }
}