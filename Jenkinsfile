pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh """echo 'Hello World and JENKINS!!!'
                    echo 'Multiline shell steps works too'"""
            }
        }
    }
}